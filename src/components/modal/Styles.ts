import { StyleSheet, Platform } from "react-native";

const styles = StyleSheet.create({
  overlayView: {
    position: "absolute",
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
  },
});

export default styles;
